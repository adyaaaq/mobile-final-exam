import 'package:flutter/material.dart';

class WishlistsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Wishlists'),
      ),
      body: Center(
        child: Text('Wishlists Page'),
      ),
    );
  }
}
